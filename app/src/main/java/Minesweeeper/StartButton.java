package Minesweeeper;
import java.awt.*;


/* public class StartButton extends Component implements Accessible{

    public StartButton(){

            // create instance of frame with the label   
    Frame f = new Frame("Button Example");    
  
    // create instance of button with label  
    Button b = new Button("Click Here");   
  
    // set the position for the button in frame   
    b.setBounds(50,100,80,30);    
  
    // add button to the frame  
    f.add(b);    
    // set size, layout and visibility of frame  
    f.setSize(400,400);    
    f.setLayout(null);    
    f.setVisible(true);     
}   
 */

/**
 * bad practice, but this could be the alternative way of starting + storing usernames :
        Scanner sc = new Scanner(System.in);
        System.out.println("Dear player, please enter your desired username to begin");
        String name = sc.nextLine();
        if (! name.equals(null)){
            System.out.println("player username: " + name);
        }
 */
    
    



//}
